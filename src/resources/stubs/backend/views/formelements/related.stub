<div class="form-group row">
  	<label class="col-md-2 col-form-label" for="{{key}}[]">{{display}}</label>
  	<div class="col-md-10">
		<select multiple class="form-control select2" id="{{key}}" name="{{key}}[]">
			@foreach(${{parentModule}} as ${{parentModuleSinglular}})
				<option value="{{ ${{parentModuleSinglular}}->id }}" {{ (collect(old('{{key}}'))->contains(${{parentModuleSinglular}}->id)) ? 'selected':'' }}>{{ ${{parentModuleSinglular}}->title }}</option>
			@endforeach
		</select>
  	</div>
</div>