<div class="{{inputRowCLasses}}">
  	<label class="{{labelClasses}}" for="{{key}}">{{label}}</label>
  	<div class="{{inputContainerClasses}}">
		<select class="{{inputClasses}} select2" id="{{key}}" name="{{key}}">
			<option>Please select</option>
			@foreach(${{relatedModule}} as ${{relatedModelLowercase}})
				<option value="{{ ${{relatedModelLowercase}}->id }}">{{ ${{relatedModelLowercase}}->title }}</option>
			@endforeach
		</select>
  	</div>
</div>
