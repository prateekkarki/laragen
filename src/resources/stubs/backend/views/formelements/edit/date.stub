<div class="{{inputRowCLasses}}">
    <label class="{{labelClasses}}" for="{{key}}">{{label}}</label>
    <div class="{{inputContainerClasses}}">
        <input class="{{inputClasses}} datepicker" type="text" name="{{key}}" id="{{key}}" placeholder="{{label}}" value="{{ ${{modelNameLowercase}}->{{key}} }}" {{options}}>
    </div>
</div>
