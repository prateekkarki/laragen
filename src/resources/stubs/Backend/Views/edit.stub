@extends('backend.layouts.main')

@section('content')
    <h1>Update {{moduleName}}</h1>

    <form action="{{ route('backend.{{moduleName}}.update', ${{modelNameLowercase}}) }}" method="PUT">
    	@csrf
		@include('backend.{{moduleName}}._form')
		<button class="btn btn-primary" type="submit">
			<i class="fa fa-save"></i> Save
		</button>
    </form>
@endsection